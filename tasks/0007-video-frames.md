# Кадр видео: середина в квадрате, целиком в «Сейчас играет»

Статус: сделано

Те же задания: `melogoldAndroid/tasks/0005-video-frames.md` и `melogoldiOSmacOS/tasks/0008-video-frames.md`.

## 1. Что нужно пользователю

Пользователь включил «Never Gonna Give You Up» (`dQw4w9WgXcQ`). В квадратной обложке остались надпись и плечо Рика, а всё справа, с лицом, было вырезано. Его слова: если бы брали середину или честно показывали прямоугольник, было бы честно.

Второе замечание: у видео-«статики» («GORILLA GLUE, LIL NAKUR – БАРМАЛЕЙ (Сингл, 2026)», `LLhpBVFh2Zg`) обложка сингла квадратная, а стоит в кадре 16:9 с чёрными полями по бокам. В «Сейчас играет» она показывалась прямоугольником с полями. Его слова: чёрные полосы нужно обрезать.

## 2. Решение (одинаково на всех клиентах)

- **Квадрат:** у кадра видео 16:9 берётся центральный квадрат, а не левый или верхний край. Это касается строк, карточек, панели плеера, мини-плеера, очереди и шапок экранов.
- **«Сейчас играет»:**
  - у кадра видео (`i.ytimg.com/vi/<id>/…`) большая обложка показывает кадр целиком, прямоугольником 16:9;
  - у песен обложка остаётся квадратом.
- **Без чёрных полос:** до 320 px — `mqdefault.jpg`, крупнее — `hq720.jpg` (`Thumbnails.Sized`).
- **Резкость:** в квадрате кадр видео декодируется по высоте, то есть по короткой стороне.
- **Чёрные поля срезаются по пикселям.** Кадр видео (`i.ytimg.com/vi/<id>/…`) бывает с полями: обложка сингла в кадре 16:9 — поля по бокам, превью 4:3 — сверху и снизу. Поля срезаются, когда картинка попадает в кэш, поэтому без полей она показывается везде: в строках, в плеере и в цвете по обложке.
  - **Правило, как `FrameBars` у Windows** (`melogoldWindows/src/Melogold.Core/Music/FrameBars.cs`, тесты `FrameBarsTests`):
    - строка или столбец — поле, если у 98 % пикселей каждый канал ≤ 28;
    - поля срезаются, только если вместе они не меньше 3 % стороны;
    - поля одинаковые с двух сторон: разница не больше 3 % стороны;
    - после обрезки остаётся не меньше 40 % стороны.
  - **Тёмная сцена самого видео полем не считается:** тёмен обычно один край, а не оба поровну.
  - Цвет по обложке уже срезает поля мягче (`ArtworkColorScheme.kt` у Android). Картинке нужно правило строже.
- **Форма в «Сейчас играет»** зависит от картинки после обрезки, а не от адреса:
  - шире 1,2:1 — прямоугольник 16:9;
  - иначе — квадрат. Обложка сингла из видео-«статики» становится квадратом.

## 3. Windows

- **Причина:**
  - у `Image` с `Stretch="UniformToFill"` желаемый размер больше места;
  - при выравнивании `Stretch` лишняя ширина уходила вправо и обрезалась, как в WPF, и от кадра оставался левый квадрат.
- **Исправление:**
  - у всех обложек стоит `HorizontalAlignment="Center"` и `VerticalAlignment="Center"`: строки, карточки, панель плеера, очередь, мини-плеер, шапки и «Сейчас играет»;
  - `Images.From` декодирует кадр видео по высоте;
  - очередь берёт адрес через `Thumbnails.Sized`, а не `hqdefault` 4:3 с полосами;
  - `NowPlayingView`: форма обложки — по пикселям картинки (`BitmapImage.ImageOpened`), шире 1,2:1 — 9/16 ширины;
  - поля срезает `VideoFrames.TrimBarsAsync` при записи в `ImageCache` (правило — `FrameBars` в Core). У кадров видео в ключе кэша стоит «#frame», чтобы не брать файлы, сохранённые до обрезки.

## 4. Проверка

- `dQw4w9WgXcQ`:
  - в панели плеера лицо по центру;
  - в «Сейчас играет» кадр целиком: слева надпись, справа Рик.
- `LLhpBVFh2Zg` («Бармалей»): в «Сейчас играет» квадратная обложка без чёрных полей.
- `FrameBarsTests`: квадрат в 16:9, превью 4:3, кадр без полей, тёмная сцена с одной стороны, почти чёрный кадр, тонкая кромка, шум JPEG в поле.
