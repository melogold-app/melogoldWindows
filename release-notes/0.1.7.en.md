- The window fits any width: at least 500×500, in a narrow window the player bar takes two rows (seek bar on top), and what does not fit moves to "…".
- Search in the title bar stays visible in a narrow window; album and playlist headers, the filter and the Library cards adapt to the width.
- Icons in rows line up in columns; the stray "Ctrl+F" tooltip is gone.
